{
    "name": "root",
    "private": true,
    "scripts": {
        "postinstall": "lerna bootstrap",
        "build:abc": "lerna run --scope @yume-chan/adb-backend-webusb build",
        "build:watch": "lerna run --scope @yume-chan/adb-backend-webusb --stream build:watch",
        "build": "lerna run --scope demo --stream build",
        "start:demo": "lerna run --scope demo --stream start",
        "test": "lerna run --stream --no-bail coverage"
    },
    "devDependencies": {
        "lerna": "3.22.1"
    },
    "dependencies": {
        "url-loader": "^4.1.1"
    },
    "engines": {
        "node": "15.x"
    }
}